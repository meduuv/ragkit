[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ragkit"
version = "0.1.0"
description = "Simple in-memory retrieval helpers"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/ragkit"]
